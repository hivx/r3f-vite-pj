import React from 'react';
import { createRoot } from 'react-dom/client';

// import App from '@/App';
import { MultiTest } from '@/MultiTest';

createRoot(document.getElementById('root')!).render(<MultiTest />);
